<TMU|<tuple|1.1.0|2025.2.0-beta1>>

<style|<tuple|generic|chinese|number-europe|python>>

<\body>
  <\session|python|conda_plot>
    <\output>
      Python 3.13.9 [D:\\miniconda3\\envs\\plot\\python.exe]

      Python plugin for TeXmacs.

      Please see the documentation in Help -\<gtr\> Plugins -\<gtr\> Python
    </output>

    <\input>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|input>
      from sympy import *

      init_printing(use_unicode=True)

      from sympy.abc import a, e, k, n, r, t, x, y, z, T, Z

      from sympy.abc import beta, omega, tau

      f, g = symbols('f, g', cls=Function)
    </input>

    <\unfolded-io>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|unfolded-io>
      from sympy.codegen.ast import Assignment

      Assignment(x, y + 1)
    <|unfolded-io>
      <math|<math-up|Assignment><around*|(|x,y+1|)>>
    </unfolded-io>

    <\unfolded-io>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|unfolded-io>
      expr = (Rational(-1, 2) * Z * k * (e**2) / r)

      expr
    <|unfolded-io>
      <math|-<frac|Z*e<rsup|2>*k|2*r>>
    </unfolded-io>

    <\unfolded-io>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|unfolded-io>
      x_ = Function('x')

      expr = x_(n*T) * sin((t - n*T) / T)

      expr = expr / ((-T*n + t) / T)

      expr
    <|unfolded-io>
      <math|<frac|T*x*<around*|(|T*n|)>*sin <around*|(|<frac|-T*n+t|T>|)>|-T*n+t>>
    </unfolded-io>

    <\unfolded-io>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|unfolded-io>
      import sympy as sp\ 

      x, y = sp.symbols('x y')

      sympy_dict = {

      \ \ \ \ 'derivative': sp.diff(x**2, x),

      \ \ \ \ 'integral': sp.Integral(sp.sin(x), x),

      \ \ \ \ 'equation': sp.Eq(x + y, 5)

      }

      sympy_dict
    <|unfolded-io>
      <math|<around*|{|<with|math-font-family|tt|mode|text|derivative>:2*x,<with|math-font-family|tt|mode|text|equation>:x+y=5,<with|math-font-family|tt|mode|text|integral>:<big|int>sin <around*|(|x|)>*<space|0.17em>d*x|}>>
    </unfolded-io>

    <\unfolded-io>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|unfolded-io>
      sympy_set = {x**2, sp.cos(x), sp.exp(x)}

      sympy_set
    <|unfolded-io>
      <math|<around*|{|x<rsup|2>,e<rsup|x>,cos <around*|(|x|)>|}>>
    </unfolded-io>

    <\input>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|input>
      # List 包含各种SymPy对象

      sympy_list = [

      \ \ \ \ x**2, \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ # 幂运算

      \ \ \ \ sp.sin(x) + sp.cos(y), \ \ \ \ \ \ \ \ # 三角函数组合

      \ \ \ \ sp.diff(x**3, x), \ \ \ \ \ \ \ \ \ \ \ \ \ # 导数: 3*x**2

      \ \ \ \ sp.Integral(sp.exp(-x), x), \ \ \ # 积分

      \ \ \ \ sp.Eq(x + y, 10), \ \ \ \ \ \ \ \ \ \ \ \ \ # 方程

      \ \ \ \ sp.Matrix([[1, x], [y, 2]]), \ \ # 符号矩阵

      \ \ \ \ sp.Rational(3, 4), \ \ \ \ \ \ \ \ \ \ \ \ # 有理数

      \ \ \ \ sp.series(sp.log(1+x), x, 0, 4) # 级数展开

      ]

      \;
    </input>

    <\unfolded-io>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|unfolded-io>
      sympy_list
    <|unfolded-io>
      <math|<around*|[|x<rsup|2>,sin <around*|(|x|)>+cos <around*|(|y|)>,3*x<rsup|2>,<big|int>e<rsup|-x>*<space|0.17em>d*x,x+y=10,<around*|[|<tabular*|<tformat|<table|<row|<cell|1>|<cell|x>>|<row|<cell|y>|<cell|2>>>>>|]>,<frac|3|4>,x-<frac|x<rsup|2>|2>+<frac|x<rsup|3>|3>+O<around*|(|x<rsup|4>|)>|]>>
    </unfolded-io>

    <\input>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|input>
      # Tuple 包含各种SymPy对象

      sympy_tuple = (

      \ \ \ \ sp.sqrt(x), \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ # 平方根

      \ \ \ \ sp.exp(x) * sp.log(y), \ \ \ \ \ \ \ \ \ # 指数和对数

      \ \ \ \ sp.Limit(sp.sin(x)/x, x, 0), \ \ \ # 极限

      \ \ \ \ sp.Sum(x**2, (x, 1, n)), \ \ \ \ \ \ # 求和

      \ \ \ \ sp.factorial(5), \ \ \ \ \ \ \ \ \ \ \ \ \ \ # 阶乘

      \ \ \ \ sp.I, \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ # 虚数单位

      \ \ \ \ sp.pi, \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ # 圆周率

      \ \ \ \ sp.oo \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ # 无穷大

      )
    </input>

    <\unfolded-io>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|unfolded-io>
      sympy_tuple
    <|unfolded-io>
      <math|<around*|(|<sqrt|x>,e<rsup|x>*log <around*|(|y|)>,lim<rsub|x\<to\>0<rsup|+>><around*|(|<frac|sin <around*|(|x|)>|x>|)>,<big|sum><rsub|x=1><rsup|n>x<rsup|2>,120,i,\<pi\>,\<infty\>|)>>
    </unfolded-io>

    <\input>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|input>
      \;
    </input>
  </session>

  \;

  dict类型：

  <\enumerate-circle>
    <item>键为特殊值，值为普通值

    <item>键为普通值，值为特殊值

    <item>键与值都是特殊值
  </enumerate-circle>

  <\session|python|conda_plot>
    \;

    <\input>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|input>
      from sympy import *

      init_printing(use_unicode=True)

      from sympy.abc import a, e, k, n, r, t, x, y, z, T, Z

      from sympy.abc import beta, omega, tau

      f, g = symbols('f, g', cls=Function)
    </input>

    <\input>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|input>
      a = Matrix([[1, 2], [2, 2]]).eigenvals()
    </input>

    <\unfolded-io>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|unfolded-io>
      a
    <|unfolded-io>
      <math|<around*|{|<frac|3|2>-<frac|<sqrt|17>|2>:1,<frac|3|2>+<frac|<sqrt|17>|2>:1|}>>
    </unfolded-io>

    <\unfolded-io>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|unfolded-io>
      import sympy as sp\ 

      x, y = sp.symbols('x y')

      sympy_dict = {

      \ \ \ \ 'derivative': sp.diff(x**2, x),

      \ \ \ \ 'integral': sp.Integral(sp.sin(x), x),

      \ \ \ \ 'equation': sp.Eq(x + y, 5)

      }

      sympy_dict
    <|unfolded-io>
      <math|<around*|{|<with|math-font-family|tt|mode|text|derivative>:2*x,<with|math-font-family|tt|mode|text|equation>:x+y=5,<with|math-font-family|tt|mode|text|integral>:<big|int>sin <around*|(|x|)>*<space|0.17em>d*x|}>>
    </unfolded-io>

    <\unfolded-io>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|unfolded-io>
      from sympy import Matrix, symbols

      \;

      M = Matrix([[1, 2], [2, 2]])

      # 获取特征值和特征向量

      eigenvects = M.eigenvects()

      \;

      # 构建字典：特征值 -\<gtr\> 特征向量

      eigen_dict = {}

      for eigval, multiplicity, eigenvectors in eigenvects:

      \ \ \ \ eigen_dict[eigval] = eigenvectors[0] \ # 取第一个特征向量

      \;

      eigen_dict
    <|unfolded-io>
      <math|<around*|{|<frac|3|2>-<frac|<sqrt|17>|2>:<around*|[|<tabular*|<tformat|<table|<row|<cell|-<frac|<sqrt|17>|4>-<frac|1|4>>>|<row|<cell|1>>>>>|]>,<frac|3|2>+<frac|<sqrt|17>|2>:<around*|[|<tabular*|<tformat|<table|<row|<cell|-<frac|1|4>+<frac|<sqrt|17>|4>>>|<row|<cell|1>>>>>|]>|}>>
    </unfolded-io>

    <\input>
      \<gtr\>\<gtr\>\<gtr\>\ 
    <|input>
      \;
    </input>
  </session>
</body>

<\initial>
  <\collection>
    <associate|page-medium|paper>
    <associate|page-screen-margin|false>
  </collection>
</initial>
