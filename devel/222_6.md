# 222_6

## 如何测试
1. 点击`编辑->查找`，切换为独立窗口，输入`111`。
2. 新建标签页，此时辅助窗口消失了，再切换为上一个标签页，查找窗口里面输入框有`111`。
3. 在新标签页中点击`编辑->替换`，切换为独立窗口，输入框中输入`222`，替换框中输入`333`。
4. 切换为旧标签页，查找框中是`111`，关闭该辅助窗口。
5. 切换为新标签页，此时查找框中是`222`，替换框中是`333`。
6. 切换为旧标签页，此时没有辅助窗口出现

## 2025/12/26 切换标签页后实时更新搜索替换辅助窗口的状态