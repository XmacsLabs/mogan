# 202_51: `文件->导入->纯文本...` 窗口修改
## 如何测试
菜单栏**文件->导入->纯文本...**，检查弹窗是否有*.txt和*（所有格式）两种格式。
菜单栏**插入->会话->手动选择路径...**，点击浏览按钮，查看是否默认为 All files。

## 2025/12/08
### What
上次直接修改了 generic 选项，这影响到 All Format 的选择，因此新增 text 格式供纯文本选项使用。

## 2025/09/26
### What
纯文本应该是 txt 之类的格式，现提供*.txt和*（所有格式）两种格式。
