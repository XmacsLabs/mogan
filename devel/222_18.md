# 222_18 隐藏插入会话菜单中不可用的菜单项

## 如何测试
1. 点击 `插入` -> `会话`，查看会话菜单，确认菜单中不再显示 "远程..." 和 "其它..." 两个菜单项
3. 点击 焦点工具栏中 `插入交互会话` 按钮，确认弹出的菜单中也不显示 "远程..." 和 "其它..." 两个菜单项

## 2026/01/08 隐藏插入会话菜单中不可用的菜单项

### What
在 `插入` -> `会话` 菜单中，"Remote"（远程会话）和 "Other"（其他）两个菜单项功能不可用，使用 `assuming #f` 将其隐藏，避免用户误操作。