# 214_10: 默认开启 preview-ref 插件

## 如何测试

- 创建一个新的 `.tmu` 文档，在焦点工具栏检查是否已经加载 `preview-ref`插件
- 点击焦点工具栏的 `preview-ref` 按钮，看是否可以取消。

## 2025/12/18

### What

+ 默认开启`preview-ref`插件

### Why

目前`preview-ref`插件是默认关闭的，需要用户手动开启，然而这个插件0学习成本且非常好用，应该给用户默认开启