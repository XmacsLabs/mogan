# 204_10: 新增tmudoc样式

## 测试验证
## 2025/11/30
- 运行程序，打开任意内置文档，将文档的样式改为tmudoc样式（ `文档` -> `样式` -> `文档` -> `墨干` -> `tmudoc`）
查看文章开头处的TeXmacs logo是否变为墨干logo，同时查看文档中是否出现乱码。
注意：在tmudoc样式下的文档，tmdoc-title 也会变成 mogan的logo,而在tmdoc样式下，mogandoc-title 将加载不出来

### Why
原来的文档的标题用的是TeXmacs的logo