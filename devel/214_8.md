# 214_8: binary插件的文档从tm改为tmu

## 如何测试

+ 确认文件 `TeXmacs/plugins/binary/doc/binary.en.tmu` 存在（英文版）
+ 确认文件 `TeXmacs/plugins/binary/doc/binary.zh.tmu` 存在（中文版）
+ 确认文件 `TeXmacs/plugins/binary/doc/binary.en.tm` 已被删除（旧格式）
+ 确认文件 `TeXmacs/plugins/binary/doc/binary.zh.tm` 已被删除（旧格式）

## 2025/11/3

### What

+ tm改为tmu    

### Why

+ 统一文档格式，`.tmu` 是新的标准文档格式