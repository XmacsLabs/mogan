# 204_19 simsun 字体使用内置的伪粗体而不是直接替换为 simhei

## 如何测试
- 测试文档：Texmacs/tests/tmu/204_19.tmu
- 将字体设置为 simsun（macos默认没有），选中后点击工具栏里的加粗，看是否变粗并仍是宋体，而不是黑体

## 2025/12/16
### What
src\Graphics\Fonts\poor_bold.cpp 里已经写好了关于伪粗体的代码，现在单独让 simsun 使用伪粗体而不是直接替换成 simhei