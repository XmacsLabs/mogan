# 222_5

## 如何测试
1. 按下`编辑->查找`，更换为独立窗口，在查找框输入`111`，关闭查找框，新建标签页，再次打开查找框，此时里面没有任何内容。
2. 关闭查找框，按下`编辑->替换`，更换为独立窗口，在查找框输入`111`，关闭查找框，新建标签页，再次打开查找框，此时里面没有任何内容。
3. 将一个标签页拉出到一个新的窗口。两个窗口都打开查找框，此时两个查找框是独立的，能分别进行独立的查找互不影响。
4. 两个窗口都打开替换框，此时两个替换框是独立的，能分别进行独立的替换查找互不影响。

## 2025/12/25 使替换查找辅助窗口与标签页一一对应