# 222_17 统一颜色参数菜单的默认项文案

## 如何测试
1. 点击`文档`->`颜色`->`背景色`，以及`文档`->`颜色`->`前景色`，确认弹出的颜色选择菜单中默认项显示为`无`。
2. 选中任意一段文本，点击`高亮文本`，并打开颜色选择菜单，确认菜单中默认项显示为`无`。

## 2025/01/08 统一颜色参数菜单的默认项文案为 None

### What
颜色类型参数的子菜单中，恢复默认值的菜单项原先显示为 "默认"，对于颜色参数而言 "无" 更能准确表达其语义。