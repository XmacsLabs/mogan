# 200_39 xmake run 的时候不触发构建
## 2025/12/9
### Why
之前xmake r stem的时候会触发构建，导致每次运行 xmake r stem 特别慢。

其实这个功能本身没问题，但是我们现在xmake在`3rdparty/qwindowkitty`的构建脚本有缺陷，多次`xmake b stem`会重复构建。

### How
参考文档：
https://xmake.io/api/description/builtin-policies.html#run-autobuild
